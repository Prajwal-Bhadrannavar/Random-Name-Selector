function whosPaying(names) {
 var number = (names.length);
 var lucky = Math.floor(Math.random() * number);
 
 
   var result = (names[lucky] +" is going to buy lunch today!"); 
    
return result;
 
}
